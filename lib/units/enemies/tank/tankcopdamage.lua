TankCopDamage = TankCopDamage or class(CopDamage)
function TankCopDamage:damage_melee(attack_data)
	return
end
function TankCopDamage:sync_damage_melee(attacker_unit, damage_percent, i_body, hit_offset_height)
	return
end
